{
  "name": "my-planner",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "node scripts/check-syntax.mjs",
    "docs:check": "node scripts/check-doc-links.mjs",
    "build": "node scripts/check-syntax.mjs && node scripts/check-doc-links.mjs"
  }
}
