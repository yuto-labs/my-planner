{
  "name": "my-planner",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "node scripts/check-syntax.mjs",
    "build": "node scripts/check-syntax.mjs"
  }
}
